# name: CI

# on:
#   push:
#     branches: [main, master]
#   pull_request:
#     branches: [main, master]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9

#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'pnpm'
#           cache-dependency-path: pnpm-lock.yaml

#       - name: Install and build
#         run: |
#           pnpm install
#           pnpm run build

#       - name: Docker build (API gateway)
#         run: docker build -f apps/api-gateway/Dockerfile .

#   deploy-skipped-without-secrets:
#     if: false
#     runs-on: ubuntu-latest
#     steps:
#       - name: Placeholder
#         run: echo "Enable ACR login + kubectl when AZURE_CREDENTIALS and kubeconfig are configured"

null
