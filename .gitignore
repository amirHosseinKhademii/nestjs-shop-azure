node_modules
dist
.env
.env.example
.env.*
# Real Secret manifests must never be committed. The .example file is the
# tracked template; CI seeds the actual Secret from GitHub Repository Secrets.
infra/k8s/secrets.yaml
infra/k8s/secrets.yml
infra/k8s/secrets.local.yaml
coverage
.turbo
*.log
.DS_Store
.terraform
*.tfstate
*.tfstate.*
.terraform.lock.hcl
terraform.tfvars

# First-commit scope: only apps/user-svc is tracked; opt in other paths later.

k8s-local/
terraform/
docker-compose.yml
docker-compose.yml
secrets.yaml